#include <stdio.h>
#include <syslog.h>
#include <unistd.h>
#include <iostream>

int main()
{

    // Append data to the file
    std::cout << "The daemon is running that." << std::endl;

    return 0;
}
