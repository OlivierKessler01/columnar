/*
 *  ANSI-SQL parser.
 *  
 *  Given a vector of tokens, returns the grammar of the request, or fails
 *  if the grammar is broken.
 *
 *	Author: Olivier Kessler <olivier.kessler@protonmail.com>
 */
#include "../scanner/analyzer.h"
#include <sys/types.h>

ssize_t parse(token* tokens, int token_arr_len)
{
    return 0;
}
